# Frontend (Flutter) configuration — see README.
# The backend URL is auto-detected; override via dart-define if needed:
#   flutter run -d chrome --dart-define=BACKEND_BASE_URL=http://127.0.0.1:8081
# BACKEND_BASE_URL=http://127.0.0.1:8081
